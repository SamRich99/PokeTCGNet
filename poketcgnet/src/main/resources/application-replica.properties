# Profilo per testare/dimostrare il replica set MongoDB locale (rs0, 3 nodi).
# Setup e script di avvio in database/replica/. Attivare con:
#   $env:SPRING_PROFILES_ACTIVE = "replica"   (poi avviare l'app normalmente)
# Tutti gli altri properties restano quelli di application.properties (default).
spring.mongodb.uri=mongodb://localhost:27018,localhost:27019,localhost:27020/poketcgnet?replicaSet=rs0&w=1&readPreference=nearest
